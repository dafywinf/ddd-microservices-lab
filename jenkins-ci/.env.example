# Copy this file to .env and replace the value with your actual SSH public key.
# Generate a key pair with: ssh-keygen -t rsa -b 4096 -f ~/.ssh/jenkins_agent
# Then paste the contents of ~/.ssh/jenkins_agent.pub below.
JENKINS_AGENT_SSH_PUBKEY=ssh-rsa AAAA...your-public-key...
